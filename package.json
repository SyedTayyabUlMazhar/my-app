{
  "name": "my-app",
  "version": "0.0.1",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "nohoist": [
      "**/react",
      "**/react-dom",
      "**/react-native",
      "**/react-native/**"
    ]
  },
  "scripts": {
    "reset": "find . -type dir -name node_modules | xargs rm -rf && rm -rf yarn.lock",
    "mobile:start": "yarn workspace @my-app/mobile start",
    "android": "yarn workspace @my-app/mobile android",
    "ios": "yarn workspace @my-app/mobile ios"
  }
}
